import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* The floating dev badge sits on top of the footer links at phone width. */
  devIndicators: false,
};

export default nextConfig;
